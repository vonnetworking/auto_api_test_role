pipeline {
  agent any
  parameters {
    text(name: 'DATA', defaultValue: null, description: 'json data payload') }
  }
  stages {
    stage('Read Data') {
      steps {
        echo 'Hello World!'
      }
    }
  }
}
